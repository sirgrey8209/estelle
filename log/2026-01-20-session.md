# 2026-01-20 작업 로그

## 완료된 작업

### 1. PM2 기반 Pylon 프로세스 관리
- `ecosystem.config.js` 생성
- `scripts/install-pm2.ps1` 생성
- docs 업데이트 (Task Scheduler → PM2)

### 2. nexus → estelle 리네이밍
- Mobile/Desktop: GitHub release URL 수정
- deploy.ps1: repo URL 수정
- 기타 주석/타이틀 수정

### 3. Desktop 연결 안정화
- React.StrictMode 제거 (HMR 문제 해결)
- WebSocket을 모듈 레벨에서 관리

### 4. Mobile Deploy 버튼 추가
- Deploy 버튼 (office-pc 우선)
- RelayClient.sendDeployRequest() 추가
- MainViewModel.requestDeploy() 추가
- deviceId를 'mobile'로 단순화

### 5. Relay/Pylon 원격 배포 기능
- Relay: deployRequest 메시지 처리
- Pylon: handleRemoteDeploy() 추가

### 6. Android 로컬 빌드로 전환
- deploy.ps1: GitHub Actions → 로컬 Gradle 빌드
- build.gradle.kts: version.properties에서 버전 읽기
- build.gradle.kts: signingConfigs 추가
- local.properties: 서명 정보 저장

### 7. Android 빌드 환경 설정 (home-pc)
- Java 17 (OpenJDK) 설치: `winget install Microsoft.OpenJDK.17`
- Android Studio 설치: `winget install Google.AndroidStudio`
- Android SDK 설치 (cmdline-tools 사용)
  - platform-tools
  - platforms;android-34
  - build-tools;34.0.0
- SDK 라이선스 수락 (licenses 폴더에 파일 생성)
- `build-release.cmd` 생성 (JAVA_HOME, ANDROID_HOME 설정)
- local.properties: sdk.dir 및 keystore 경로 설정
- **빌드 성공**: `app/build/outputs/apk/release/app-release.apk`

### 8. 캐릭터 시스템 추가
우주/별 테마의 캐릭터로 각 디바이스 표현:

| 캐릭터 | 아이콘 | 역할 | deviceId |
|--------|--------|------|----------|
| Estelle | 💫 | Relay | - |
| Stella | ⭐ | 회사 PC | `stella` |
| Selene | 🌙 | 집 PC | `selene` |
| Lucy | 📱 | Mobile | `lucy` |

- Desktop/Mobile UI에 캐릭터 아이콘/이름 표시
- `docs/characters.md` 캐릭터 설정 문서 추가
- `.env.stella`, `.env.selene` 환경설정 파일 분리

## 다음 단계
- [ ] 회사 PC에서 `copy .env.stella .env` 실행
- [ ] Relay 배포 (fly deploy)
- [ ] Pylon 재시작 (PM2)
- [ ] 전체 테스트
