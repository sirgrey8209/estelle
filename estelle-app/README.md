# Estelle App

통합 클라이언트 (Windows, Android, Web)

## 실행

```bash
# Web
flutter run -d web-server --web-port=8080

# Windows
flutter run -d windows

# Android
flutter run -d android
```

## 빌드

```bash
flutter build windows
flutter build apk
flutter build web
```
