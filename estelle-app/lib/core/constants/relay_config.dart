class RelayConfig {
  RelayConfig._();

  static const String relayUrl = 'wss://estelle-relay.fly.dev';
  static const String appVersion = '1.0.0';

  // deviceType: app (Relay에서 deviceId 자동 발급)
  static const String deviceType = 'app';
}
